class Solution {
    public int reverse(int x) {
            

         try {
        return x>0?Integer.parseInt(new StringBuilder(String.valueOf(x)).reverse().toString()):-Integer.parseInt(new StringBuilder(String.valueOf(-x)).reverse().toString());
        } catch (Exception e) {
            return 0;
        }
}
}
